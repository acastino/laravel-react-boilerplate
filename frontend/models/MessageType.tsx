type MessageType = {
  id: number;
  // from_id: number;
  recipients: string[];
  subject: string;
  message: string;
  // created_at: string;
  // updated_at: string;
};

export default MessageType;
